<h1><%= @restaurant.name %> <%= '⭐️' * @restaurant.rating %></h1>
<p><%= @restaurant.address %> </p>

<%= link_to "Delete #{@restaurant.name}",
            restaurant_path(@restaurant),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
